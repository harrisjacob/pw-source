import React, { Component } from 'react';
import Grid from "@material-ui/core/Grid";

export default class DuckAnimation extends Component {
	constructor(props){
		super(props);
	};

	render(){
		return(
			<h1>Duck Animation</h1>
		);
	};
}